/**
 * Organization & Team Settings page.
 * Manages team roster, RBAC policies, billing, and enterprise configuration.
 * Renders TeamSettings with TeamRoster and RBACPolicies components.
 */
import TeamSettings from '@/pages/Organization/TeamSettings'

export default function Organization() {
  return <TeamSettings />
}
